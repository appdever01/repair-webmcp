# Private IconJar package

This directory contains the licensed IconJar package selected by the neutral `@iconjar/icons`
dependency alias in `package.json`.

Keep the tarball private. To change the icon style, copy a generated replacement package from the
private IconJar repository, update the dependency alias, run `pnpm install`, and execute the
complete validation gates.
